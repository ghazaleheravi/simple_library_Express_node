extends layout

block content
  h1= title
  p #{title}

  h1 Dynamic content 

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

  ul 
    li #[strong Books:] !{data.books_count}
    li #[strong Authors:] !{data.authors_count}
    li #[strong Book-Instances:] !{data.bookInstance_count}
    li #[strong Availabe-Book-Instance:] !{data.bookInstance_available_count}
    li #[strong Genres:] !{data.genre_count}